# tag ID: A0000
# size (oz)
# sightings per month

# is_large (large means > 5 oz)
# is_small (small means < 3 oz)
# capture(month)

class Rodent:

	def __init__(self, tag_id, size)
		self.tag_id = tag_id\
		self.size_id = size_id
		self.sightings_per_month = {}

	def is_large(self):
		# return true if size > 5 oz
		pass

	def is_small(self):
		# return true if size < 3 oz
		pass

	def plot(self):
		# return the letter of the plot in which the rodent was captured
		pass

	def capture(self, month):
		# we captured this rodent once in this month
		pass
